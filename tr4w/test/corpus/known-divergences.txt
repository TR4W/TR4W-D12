# Corpus sets whose D7<->D12 golden diff is a KNOWN, tracked behavior
# difference -- NOT a D12 conversion regression. run-golden-diff.sh reports
# these as KNOWN instead of FAIL.  Format:  <slug>  <reason>
#
# Event-source issue (TR4W-D12 issue #2, gated on the SQLite log migration):
# the SENT exchange is rebuilt from MY-station globals at export time, NOT
# stored per QSO.  /EXPORT deliberately skips ApplyStoredCommands and applies
# only COMPUTER ID (uProgramMain.pas:1067-1072), so those globals sit at their
# CFGDEF.PAS:294-295 default of '' and the sent half of the exchange is built
# from nothing.  The RECEIVED half is faithful in both sets -- logdump confirms
# it, and the diff below shows the far station's zones read correctly.
#
# CORRECTED 2026-09-01, against ref.cbr and cand.cbr rather than from memory.
# The two reasons recorded here described defects that DO NOT EXIST and a
# candidate output that no longer matched -- on the file the oracle uses to
# decide whether a difference is expected.  What was wrong:
#
#   * iaru was recorded as the sent zone "falling back to MY STATE", candidate
#     "59  FL".  There is no MY-STATE fallback in that arm
#     (uCabrilloExchange.pas:223-227).  The FL was the dangling-PChar bug since
#     fixed and documented at PostUnit.PAS:2753-2759 ("'599 001' became
#     'FL 001' across six corpus sets").  Measured today:
#
#       REF   ... NY4I    59  8      W1AW           59  8       1
#       CAND  ... NY4I    59  0      W1AW           59  8       1
#
#     MyZone is '' and StrToIntDef('', 0) yields 0 (uCabrilloExchange.pas:225).
#
#   * general_qso was recorded as "MY STATE appended to sent exchange".  It is
#     the OPPOSITE -- MY NAME is MISSING and nothing is appended.  Measured:
#
#       REF   ... W1AW/4  59  TOM          KD6RYO         59  AR
#       CAND  ... W1AW/4  59               KD6RYO         59  AR
#
#     MyName is '' at export, so SetMyEx drops it (uCabrilloExchange.pas:172).
#
# THE IARU ROWS ARE CLOSED (2026-09-02, step C3) AND THE DIAGNOSIS ABOVE WAS
# INCOMPLETE.  It said the sent zone was empty because MyZone is '' under
# /EXPORT.  The real fault was worse and not headless-only: MyZone holds the CQ
# zone, and EVERY exchange arm that sends "my zone" sent it -- including the
# contests whose exchange is the ITU zone.  MY ITU ZONE existed and no export
# path read it.  An IARU entrant submitted the wrong zone on every QSO of a real
# contest, and the corpus had this filed as a headless-export artifact.
#
# PostUnit.ZoneSentForThisContest decides from ContestsArray[Contest].ZnM, which
# is the table that already records which zone a contest is about, and falls back
# to CTY.DAT's ITU zone for our own call when MY ITU ZONE is unset -- which is
# what makes it right for an operator who never heard of the setting, and what
# makes this reference reproduce: ctyGetITUZone('NY4I') = 8, exactly what D7 sent.
#
# Keying off the exchange SHAPE would have been wrong in both directions:
# CROATIAN and GAGARIN-CUP share CQ-160's arm and are ITUZones contests; CQ-160
# is not.  So the fix reaches more contests than the one that exposed it.
general_qso_2026_w1aw4  event-source: sent name missing (MyName '' under /EXPORT; D7 sent TOM)

# ---------------------------------------------------------------------------
# 2026-08-13 -- all four rows are now DEMONSTRATED, not asserted.
#
# iaru_hf_2026_ny4i used to sit in export-d12-corpus.sh's SKIP list ("pops an
# interactive dialog"), so it produced no candidate and run-golden-diff.sh
# reported it KNOWN on the strength of this file alone.  That listing had gone
# stale: the set exports cleanly (exit 0, both artifacts, no dialog).  Its
# divergence is in the SENT zone only, while the RECEIVED zones are read
# correctly.  The example below was itself stale -- it showed FL, from a bug
# fixed since -- and is re-measured 2026-09-01 from the actual files:
#
#   D7-ref   QSO:  1800 PH 2026-07-05 1912 NY4I    59  8   W1AW  59  8   1
#   D12-cand QSO:  1800 PH 2026-07-05 1912 NY4I    59  0   W1AW  59  8   1
#   D7-ref   QSO:  1800 PH 2026-07-05 1912 NY4I    59  8   F4HQ  59  27  1
#   D12-cand QSO:  1800 PH 2026-07-05 1912 NY4I    59  0   F4HQ  59  27  1
#
# A row here now means ONLY "this set exports and the diff is tracked".  It no
# longer excuses a MISSING candidate -- that is a strict FAIL for every set,
# with no exemption.  If a set ever genuinely cannot be exported, do not add it
# here: that is a different fact and needs its own mechanism, or the oracle
# goes back to being unable to tell "differs as expected" from "produced
# nothing".
#
# STILL OPEN (P2-10): these four are a pre-existing D7 quirk, not a D12
# conversion defect -- the sent exchange is rebuilt from MY-station session
# globals at export time instead of being stored per QSO.
#
# GATED ON THE SQLite3 LOG MIGRATION (NY4I, 2026-08-13).  Do not reach for the
# obvious fix first: storing the sent exchange with the QSO widens the log
# RECORD, and that record is the fixed-layout binary .dat that these fixtures
# are D7-WRITTEN examples of.  Widening it now means a format break, a
# converter, and re-freezing all 13 fixtures -- work the SQLite3 move has to do
# once anyway.  Capture the sent exchange in that schema from the start.
#
# TRACKED AS TR4W-D12 issue #2, "Event sourcing issue in log record creation".
# The four diffs above are attached there as its concrete instances.  When it
# is fixed these rows come out and the corpus goes to 26 passed / 0 known.
